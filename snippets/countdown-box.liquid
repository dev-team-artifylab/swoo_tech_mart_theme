<div class="bg-gray-100 rounded-md p-2 min-w-[45px] text-center">
  <div class="text-lg font-bold text-gray-800">{{ number }}</div>
  <div class="text-[10px] text-gray-400 uppercase">{{ label }}</div>
</div>